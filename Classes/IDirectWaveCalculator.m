classdef IDirectWaveCalculator < handle & matlab.mixin.Heterogeneous
    properties (Access = private)

    end

    properties (Dependent)
    end

    methods
    end

    methods (Access = private)
    end
end